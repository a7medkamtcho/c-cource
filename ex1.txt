#include <cstdio>
#include <cmath>
#include <iostream>

using namespace std;

int main ()
{
    float r;
    float a;
    float v;
    float t;
    cout<<"area and volume of a sphere";
    cout<<"pleas enter the (R) ";
    cin>>r;
    t=4/3;
    v=t*3.14*pow(r,3);
    a=4*3.14*pow(r,2);
    cout<<"the area is = "<<a;
    cout<<"\nthe vloume is = "<<v;
}